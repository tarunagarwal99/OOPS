#include<stdio.h>

int main(){
    char str[100];
    printf("enter a string:\n");
    gets(str);    //take input from the user
    puts(str);    //print the output that taken from the gets method.
     return 0;
}